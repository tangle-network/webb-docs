[context.production]
  command = "yarn build && cp .netlify/_redirects public/_redirects"
  publish = "public/"

[context.deploy-preview]
  command = "yarn build && cp .netlify/_redirects public/_redirects"
  publish = "public/"

[context.dev]
  command = "yarn build && cp .netlify/_redirects public/_redirects"
  publish = "public/"
